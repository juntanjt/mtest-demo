spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:PCTDiscount;MODE=MYSQL;DB_CLOSE_DELAY=-1
    username: root
    password: root
    schema:
      - classpath:/ddl/schema.sql
    initialization-mode: always
